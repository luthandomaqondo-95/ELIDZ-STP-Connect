/**
 * Re-exports change-password. Both routes show the same password change form.
 * The email reset link goes to change-password; reset-password is kept for backwards compatibility.
 */
export { default } from './change-password';
